| #  | What                                                                                                                                        | Why                                                                                     | When                                                                                    | Where                                                                                     | Who                                                                                   | How                                                                                |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 1  | What are the fundamental data types in C (`int`, `char`, `float`, `double`, `void`) and how do they differ in size, precision, and purpose? | Why does C provide multiple primitive data types instead of a single universal type?    | When would you choose `float` over `double`, or `char` over `int`?                      | Where are these data types commonly used in system programming and embedded applications? | Who determines the actual size of a datatype — the compiler, OS, or CPU architecture? | How does the compiler allocate memory and interpret binary data for each datatype? |
| 2  | What is the difference between signed and unsigned data types in C?                                                                         | Why can using an unsigned datatype lead to unexpected bugs during comparisons or loops? | When should a programmer prefer unsigned integers over signed integers?                 | Where are unsigned datatypes heavily used in real-world software systems?                 | Who introduced signed/unsigned integer behavior in the C language specification?      | How does integer overflow behave differently in signed and unsigned variables?     |
| 3  | What happens internally when type conversion occurs between `int`, `float`, and `double`?                                                   | Why can implicit type conversion reduce program accuracy or create hidden bugs?         | When does automatic type promotion happen during arithmetic expressions?                | Where in complex calculations do precision-loss problems become critical?                 | Who performs type casting — the programmer, compiler, or processor?                   | How does explicit type casting alter the generated machine instructions?           |
| 4  | What is the role of the `sizeof()` operator in C?                                                                                           | Why is `sizeof()` considered architecture-dependent?                                    | When should developers use `sizeof()` instead of hardcoded memory sizes?                | Where does incorrect use of `sizeof()` commonly cause memory-related bugs?                | Who evaluates the `sizeof()` expression during compilation?                           | How does `sizeof()` behave differently with arrays, pointers, and structures?      |
| 5  | What are derived datatypes in C, and how are arrays, pointers, functions, and structures categorized under them?                            | Why are derived datatypes essential for building complex software systems?              | When does a programmer decide to use structures instead of arrays?                      | Where are derived datatypes heavily used in operating systems and compilers?              | Who manages the memory relationships between derived datatypes?                       | How do derived datatypes internally store and organize data in memory?             |
| 6  | What is datatype compatibility in C?                                                                                                        | Why can incompatible datatypes lead to undefined behavior?                              | When does the compiler automatically allow datatype compatibility?                      | Where are datatype mismatches most dangerous in large-scale applications?                 | Who checks datatype compatibility during compilation?                                 | How does the compiler detect and report incompatible datatype operations?          |
| 7  | What are storage classes in C (`auto`, `register`, `static`, `extern`) and how do they affect datatypes?                                    | Why does storage duration matter in variable management?                                | When should a variable be declared `static` instead of `auto`?                          | Where in memory are variables from different storage classes stored?                      | Who manages the lifecycle of variables declared with different storage classes?       | How does storage class selection impact datatype accessibility and lifetime?       |
| 8  | What is the difference between `float`, `double`, and `long double` in IEEE floating-point representation?                                  | Why does floating-point arithmetic sometimes produce inaccurate results?                | When should high-precision datatypes be mandatory in software design?                   | Where do floating-point precision errors become dangerous in industry applications?       | Who defines floating-point standards used in C implementations?                       | How are floating-point numbers encoded and processed internally by hardware?       |
| 9  | What is integer promotion in C expressions?                                                                                                 | Why can integer promotion produce unexpected arithmetic results?                        | When does the compiler automatically promote smaller datatypes like `char` and `short`? | Where are integer promotion issues commonly observed in embedded systems?                 | Who defined integer promotion rules in the ANSI C standard?                           | How does integer promotion influence CPU instruction execution?                    |
| 10 | What is the significance of the `void` datatype in C?                                                                                       | Why is `void` considered a special datatype rather than a normal type?                  | When should a function return `void` instead of another datatype?                       | Where are `void pointers` most commonly used in generic programming?                      | Who introduced generic pointer concepts using `void*`?                                | How does a `void*` pointer differ from typed pointers internally?                  |
| 11 | What are enumeration (`enum`) datatypes in C?                                                                                               | Why do enums improve readability and maintainability compared to integer constants?     | When should enums be preferred over macros using `#define`?                             | Where are enums widely used in protocol design and embedded systems?                      | Who assigns integer values to enumeration constants?                                  | How are enum variables stored and represented in memory?                           |
| 12 | What is the difference between a structure and a union in C?                                                                                | Why can unions save memory compared to structures?                                      | When should a programmer use a union instead of a structure?                            | Where are unions commonly used in low-level hardware programming?                         | Who decides the final memory size of a union?                                         | How does memory sharing work internally within unions?                             |
| 13 | What is datatype alignment and padding in C structures?                                                                                     | Why does structure padding improve CPU performance?                                     | When can padding become problematic in networking or file storage applications?         | Where in memory are padding bytes inserted by the compiler?                               | Who determines alignment rules for datatypes?                                         | How does the compiler calculate offsets and alignment boundaries?                  |
| 14 | What are type qualifiers (`const`, `volatile`, `restrict`) in C?                                                                            | Why are qualifiers important in optimizing and protecting programs?                     | When should a variable be declared `volatile`?                                          | Where are volatile variables critical in embedded and hardware-interfacing code?          | Who uses the `restrict` keyword for optimization — compiler or programmer?            | How do type qualifiers influence compiler optimizations and memory access?         |
| 15 | What are user-defined datatypes in C using `typedef`?                                                                                       | Why does `typedef` improve portability and readability of code?                         | When should programmers create aliases for existing datatypes?                          | Where are typedefs extensively used in operating systems and APIs?                        | Who benefits most from datatype abstraction using typedefs?                           | How does `typedef` differ from preprocessor macros internally?                     |
| 16 | What are the risks associated with datatype overflow and underflow?                                                                         | Why can overflow vulnerabilities lead to security exploits?                             | When are overflow conditions most likely to occur in arithmetic-heavy programs?         | Where are overflow bugs frequently found in system-level software?                        | Who is responsible for handling overflow protection — compiler or programmer?         | How can overflow detection and prevention be implemented effectively?              |
| 17 | What is the difference between compile-time and runtime datatype checking in C?                                                             | Why is C considered weakly type-safe compared to modern languages?                      | When can datatype errors escape compiler detection?                                     | Where do runtime datatype-related crashes commonly appear?                                | Who performs compile-time checks and who handles runtime behavior?                    | How can developers minimize datatype-related runtime failures?                     |
| 18 | What are pointers to different datatypes and how do they behave differently?                                                                | Why does pointer arithmetic depend on datatype size?                                    | When should a programmer use `void*` instead of typed pointers?                         | Where are datatype-specific pointers heavily used in memory manipulation routines?        | Who calculates pointer increments during arithmetic operations?                       | How does pointer arithmetic internally scale addresses based on datatype size?     |
| 19 | What is the role of datatypes in function arguments and return values?                                                                      | Why must function prototypes match datatype expectations accurately?                    | When does datatype mismatch in function calls become dangerous?                         | Where are function datatype mismatches commonly observed in legacy C code?                | Who validates datatype consistency during function calls?                             | How are function parameters passed and converted at machine level?                 |
| 20 | What are platform-dependent datatype variations in C?                                                                                       | Why can the same C program behave differently across systems?                           | When should developers use fixed-width integer types like `int32_t`?                    | Where are portability issues caused by datatype size differences most severe?             | Who defines datatype sizes for a specific compiler implementation?                    | How can programmers write portable code despite datatype variations?               |
